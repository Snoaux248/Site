
function word_node(data){
    return {letter: new Array(26)};
};

class deictionary_tree{

        constructor(){
            this.root = NULL;
        }

        insert(data){
            if(this.root === NULL){
                var newNode = new word_node();
            }
            //var newNode = new word_node();
        }
        remove(data){

        }
        insert_word(root, newNode, word){
            if(this.root == NULL){
                this.root = new Node;
            }
            for(var i = 0; i < word.length; i++){
                if(word.at(i));
            }
        }

}

function get_word(word){


}