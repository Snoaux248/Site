# Hackathon Application README

## Overview

This current repository is a heavily modified version of the code to support GitHub Sites.
There is supposed to be a Node/Express backend however some JS would have to be uncommented for it to run and virtually every every path will need to be adjusted

---
    there is a plethora of planned features several being
    
    -- full support for searching class notes
    -- automatic translation of class notes into system format
    -- the images tab
    -- front end search filtering
    -- user auth
    -- native applications for windows and macos "to avoid necessity of user auth"
    -- full textbook style notes/reading
    
    as of right now Definition retrieval works on the backend and so does saved hyper links ("no user specific support")

---
